package Assignment9;

public class Q3 {
    //A Spring configuration file is an XML file that contains the classes information.
    // It describes how those classes are configured as well as introduced to each other. The XML configuration files, however, are verbose and cleaner.
    // If it’s not planned and written correctly, it becomes very difficult to manage in big projects.
}
